# Assignment: Create a contiainer with basic nginx server

> Goal: Deploy a container to show container website

- Use the `nginx` image 
- Use `ports` to expose Nginx on 8080
- Walk though Nginx in browser at http://localhost:8080
